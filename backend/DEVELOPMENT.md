To use the microservice client during development you need to run the following command:
(If you're using Windows, run it in Git Bash)
```
./generate_client.sh
```

To setup .env file, create a new .env file in **/backend**, copy the content of **/backend/.env.example** and fill in the values.

Enjoy!